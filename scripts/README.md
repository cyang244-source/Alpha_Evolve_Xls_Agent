# Scripts

Place runnable project scripts in this directory.

Suggested contents:

- `run_agent.py`
- `PPA_script.sh`
- `extract_ppa_features.sh`

Keep this directory focused on reusable workflow code rather than generated outputs.
