# Datasets

Use this directory for small benchmark inputs that you want to publish with the project.

Recommended contents:

- a curated public subset of IR benchmarks
- a short description of where each benchmark came from
- any usage notes needed to run the workflow
